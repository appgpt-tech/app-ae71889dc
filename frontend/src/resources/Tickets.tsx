//Source code generated by AppGPT (www.appgpt.tech)
import {
  Datagrid,
  List,
  EditButton,
  Edit,
  SimpleForm,
  Create,
  SelectColumnsButton,
  DatagridConfigurable,
  TopToolbar,
  CreateButton,
  ExportButton,
  FilterButton,
  //Field controls
  BooleanField,
  DateField,
  EmailField,
  ImageField,
  NumberField,
  ReferenceField,
  TextField,
  UrlField,
  //Input controls
  BooleanInput,
  DateInput,
  EmailInput,
  ImageInput,
  NumberInput,
  ReferenceInput,
  TextInput,
  UrlInput,
  PasswordInput,
} from 'react-admin';
import { useRecordContext } from 'react-admin';
import { Grid } from '@mui/material';
const ReadOnlyPasswordField = ({ record, source }) => {
  // You can customize the way you display the password here, e.g., mask it with asterisks
  const maskedPassword = '********';

  return <span>{maskedPassword}</span>;
};
const ListActions = () => (
  <TopToolbar>
    <FilterButton />
    <CreateButton />
    <ExportButton />
    <SelectColumnsButton />
  </TopToolbar>
);
const TicketsTitle = () => {
  const record = useRecordContext();
  return <span>Tickets {record ? `"${record.id}"` : ''}</span>;
};

export const TicketsList = () => (
  <List actions={<ListActions />} filters={ResourceFilters}>
    <DatagridConfigurable>
      <NumberField source="TicketID" />
      <NumberField source="PlayersNumberChoice" />
      <TextField source="PlayersColourChoice" />
      <NumberField source="AmountBet" />
      <EditButton />
    </DatagridConfigurable>
  </List>
);

export const TicketsEdit = () => (
  <Edit title={<TicketsTitle />}>
    <SimpleForm>
      <Grid container rowSpacing={1} columnSpacing={{ xs: 1, sm: 2, md: 5 }}>
        <Grid item xs={4}>
          <NumberInput source="TicketID" />
        </Grid>
        <Grid item xs={4}>
          <NumberInput source="PlayersNumberChoice" />
        </Grid>
        <Grid item xs={4}>
          <TextInput source="PlayersColourChoice" />
        </Grid>
        <Grid item xs={4}>
          <NumberInput source="AmountBet" />
        </Grid>
      </Grid>
    </SimpleForm>
  </Edit>
);

export const TicketsCreate = () => (
  <Create>
    <SimpleForm>
      <Grid container rowSpacing={1} columnSpacing={{ xs: 1, sm: 2, md: 5 }}>
        <Grid item xs={4}>
          <NumberInput source="TicketID" />
        </Grid>
        <Grid item xs={4}>
          <NumberInput source="PlayersNumberChoice" />
        </Grid>
        <Grid item xs={4}>
          <TextInput source="PlayersColourChoice" />
        </Grid>
        <Grid item xs={4}>
          <NumberInput source="AmountBet" />
        </Grid>
      </Grid>
    </SimpleForm>
  </Create>
);

const ResourceFilters = [
  <TextInput source="q" label="Search" alwaysOn />,
  ,
  ,
  ,
  ,
];
